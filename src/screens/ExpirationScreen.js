import React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const ExpirationScreen = () => {
    return (
        <Text>Expiration Screen</Text>
    )
};

const styles = StyleSheet.create({});
export default ExpirationScreen;